"use strict";
/**
 * The lifecycle status of a {@link MeetMeUser}.
 * @memberOf enums
 * @class
 */
var MeetMeUserStates = {
    /**
     * The user joined the MeetMe room.
     */
    JOINED: 'joined',

    /**
     * The user left the MeetMe room.
     */
    LEFT: 'left'
};
Object.freeze(MeetMeUserStates);
module.exports = MeetMeUserStates;